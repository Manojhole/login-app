apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: login-app

spec:
  clusterIP: None

  selector:
    app: mysql

  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
