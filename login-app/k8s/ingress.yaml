apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: login-app-ingress
spec:
  rules:
    - host: login.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80